services:
  mysql_db:
    build: .
    container_name: mysql_practica4
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: practica4_ecommerce
    ports:
      - "3309:3306"
    volumes:
      - db_data_p4:/var/lib/mysql

volumes:
  db_data_p4:
